spring:
  datasource:
    url: jdbc:mysql://localhost:3306/corporate_db?createDatabaseIfNotExist=true
    username: root
    password: Sakarya123
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        generate_statistics: true 
        highlight_sql: true
  
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.orm.queries: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace